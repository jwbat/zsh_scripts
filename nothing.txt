tooltip: {
    xDateFormat: "%Y-%m-%d %I:%M:%S %p",
    pointFormatter: function () {
      const value = this.y;
      const formattedValue = formatNumber(value, { format: self.format });
      return `<b>${formattedValue} ${self.options.hideValueUnit ? undefined : `${self.unitStr}` }</b>`; <== new
      //return `<b>${formattedValue}</b>`;  <== formerly
    }
    //valueSuffix: this.options.hideValueUnit ? undefined : `${this.unitStr}`  <== not used now
},
